version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: yes
    ignore:
      - .gitignore
      - README.md
hooks:
  AfterInstall:
    - location: scripts/restart_server.sh
      timeout: 300
      runas: root
